# Desktop (macOS / Windows) tester deps. Pure-Python / cross-platform wheels.
pyserial>=3.5
esptool>=4.7
pytest>=8.0

# Raw HID input reports (the `hid` marker -- test_buttons.py etc). Reads the
# report straight off the bonded device; bundles libhidapi in the wheel. This
# is the "what the descriptor + firmware produce" path, not "what the OS input
# stack makes of it" -- that native backend (IOKit / Raw Input) comes later.
hidapi>=0.14
