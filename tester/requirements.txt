esptool>=4.7
pytest>=8
pyserial>=3.5
evdev>=1.6
pyudev>=0.24
dbus-fast>=2.21      # GATT reads (Device Info / PnP / Battery Power State) over BlueZ D-Bus
pysdl2>=0.9.16       # SDL_GameController view of the device (host/hil/sdlreport.py)
pysdl2-dll>=2.28     # bundled SDL2 .so -- has aarch64 Linux wheels, no apt/sudo needed
tomli>=2 ; python_version < "3.11"
